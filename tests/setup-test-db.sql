-- Test database setup script
-- Run this before running tests

CREATE DATABASE IF NOT EXISTS `laravel_ip_guard_test` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Grant permissions (adjust as needed for your MySQL setup)
-- GRANT ALL PRIVILEGES ON `laravel_ip_guard_test`.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;
